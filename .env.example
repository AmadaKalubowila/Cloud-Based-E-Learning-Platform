# ============================================================
# Copy this file to .env for local development:
#   cp .env.example .env
# Then fill in real values. .env is gitignored and must NEVER
# be committed. This file (.env.example) contains placeholders
# only — do not put real secrets here.
# ============================================================

SPRING_PROFILES_ACTIVE=local

# --- Local MySQL (installed on your machine) ---
DB_HOST=localhost
DB_PORT=3306
DB_NAME=elearning_db
DB_USERNAME=root
DB_PASSWORD=root

# --- JWT ---
JWT_SECRET=replace_with_a_long_random_string

# --- AWS S3 (optional locally — leave blank to skip S3 features) ---
AWS_REGION=us-east-1
AWS_S3_BUCKET=elearning-cloud-project-2026
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# --- Mail (optional locally) ---
MAIL_USERNAME=kalubowilaamada20@gmail.comelearning-cloud-project-2026
MAIL_PASSWORD=scnihtmkptbbopom
ADMIN_EMAIL=amadakalubowila@gmail.com
